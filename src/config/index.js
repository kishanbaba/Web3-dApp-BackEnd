const config = {
    port: 4000,

    // Set to 'production' to disable detailed error messages and morgan logging
    nodeEnv: 'production',

    // URL of your frontend application
    corsOrigin: '*',
};

export default config;